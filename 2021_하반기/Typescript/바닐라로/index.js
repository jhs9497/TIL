let 이름 = 'kim';
// 이 변수엔 string(문자)만 들어올 수 있다!
let 어레이 = ['kim', 'park'];
// 이 배열엔 문자형만 들어올 수 있다!
let 이름or숫자 = 123;
이름or숫자 = '문자';
// 타입의 변수화
let 타입변수화 = '타입은 보통 대문자로 명명함';
// 타입 변수로 먹이기 가능
function 함수(x) {
    return x * 2;
}
let john = [122, true];
let hyunsix = { name: '현식', gender: 'Man' };
